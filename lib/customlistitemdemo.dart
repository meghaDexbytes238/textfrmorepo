import 'package:flutter/material.dart';

class CustomListItemDemo1 extends StatelessWidget {
  const CustomListItemDemo1({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body:   ListView(
        children: [
         // Costom
        ],
      ),
    );
  }
}
